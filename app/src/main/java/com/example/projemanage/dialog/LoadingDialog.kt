package com.example.baseproject.dialog

import androidx.annotation.LayoutRes
import androidx.fragment.app.DialogFragment

class LoadingDialog(@LayoutRes val layoutId: Int): DialogFragment(layoutId) {
}